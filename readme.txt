UTF-8だとデフォルトのMCPでリコンパイルできないのでSJISへ戻しています。
We are back to SJIS that you can not recompile MCP of default that it is UTF-8.

Note: The files have been converted back to UTF-8, 
But in ANSI as UTF-8 (aka UTF-8 without BOM), tested and working with MCP recompiling and reobfuscation.